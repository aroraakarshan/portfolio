---
import ParticleCanvas from '../components/ParticleCanvas';
import '../styles/global.css';
import data from '../data/portfolio.json';
---
<html lang="en">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>{data.name} — {data.title}</title>
  <meta name="description" content={data.heroDescription} />
  <link rel="icon" href="/favicon.svg" />
</head>
<body>
  <!-- Custom Cursor -->
  <div class="cursor" id="cursor"></div>
  <div class="cursor-dot" id="cursor-dot"></div>

  <!-- Particles -->
  <ParticleCanvas client:only="react" />

  <!-- Loading Screen -->
  <div class="loader" id="loader">
    <div class="loader-text">
      {data.name.split('').map((char: string) => <span>{char === ' ' ? '\u00A0' : char}</span>)}
    </div>
    <div class="loader-line"></div>
  </div>

  <!-- Nav -->
  <nav class="nav" id="nav">
    <a href="/" class="nav-brand">
      <div class="nav-dot"></div>
      <span>{data.name}</span>
    </a>
    <div class="nav-links">
      <a href="#about">About</a>
      <a href="#experience">Experience</a>
      <a href="#projects">Projects</a>
      <a href="#services">Mentorship</a>
      <a href="#contact">Contact</a>
      <a href={data.social.linkedin} target="_blank" rel="noopener">LinkedIn</a>
      <a href={data.services.bookingLink} target="_blank" rel="noopener" class="nav-cta">🎯 Book a Session</a>
    </div>
  </nav>

  <!-- ═══════ HERO ═══════ -->
  <section class="hero" id="hero">
    <p class="hero-label" id="hero-label">{data.tagline}</p>
    <h1 class="hero-title" id="hero-title">
      {data.heroHeadline.lines.map((line: any) => (
        <span class="line">
          {line.words.map((word: string) => (
            <span class="word">
              <span class={`word-inner ${line.gradient ? 'gradient-text' : ''}`}>{word}</span>
            </span>
          ))}
        </span>
      ))}
    </h1>
    <p class="hero-sub" id="hero-sub">{data.heroDescription}</p>
    <div class="hero-cta" id="hero-cta">
      {data.heroCTAs.map((cta: any) => (
        <a
          href={cta.href}
          class={`btn btn-${cta.style}`}
          {...(cta.external ? { target: '_blank', rel: 'noopener' } : {})}
        >{cta.label}</a>
      ))}
    </div>
    <div class="hero-scroll" id="hero-scroll">
      <span>Scroll</span>
      <div class="scroll-bar"></div>
    </div>
  </section>

  <!-- ═══════ MARQUEE ═══════ -->
  <div class="marquee">
    <div class="marquee-track" id="marquee-track">
      {[...data.marqueeItems, ...data.marqueeItems].map((item: string, i: number) => (
        <Fragment key={i}>
          <span class="marquee-item">{item}</span>
          <span class="marquee-item marquee-dot">·</span>
        </Fragment>
      ))}
    </div>
  </div>

  <!-- ═══════ ABOUT ═══════ -->
  <section class="section" id="about">
    <div class="section-inner">
      <div class="about-layout">
        <div>
          <p class="section-label" data-reveal>About</p>
          <h2 class="section-title" data-reveal set:html={data.about.headline.replace(/\n/g, '<br/>')} />
          <p class="section-desc" data-reveal set:html={data.about.description} />
        </div>
        <div class="about-stats">
          {data.about.skills.map((skill: any) => (
            <div class="stat" data-reveal>
              <div class="stat-value" style={`color: var(--accent-${skill.color});`}>{skill.label}</div>
              <div class="stat-label">{skill.detail}</div>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════ EXPERIENCE ═══════ -->
  <section class="section" id="experience">
    <div class="section-inner">
      <p class="section-label" data-reveal>Experience</p>
      <h2 class="section-title" data-reveal>Career Timeline</h2>

      <div class="timeline">
        {data.experience.map((job: any, i: number) => (
          <div class="timeline-item" data-reveal>
            <div class="timeline-dot"></div>
            <div class="timeline-date">{job.date}</div>
            <div class="timeline-role">{job.role}</div>
            <div class="timeline-company">{job.company}</div>
            <div class="timeline-highlights">
              {job.highlights.map((chip: any) => (
                <span class={`timeline-chip ${chip.featured ? 'chip-highlight' : ''}`}>{chip.label}</span>
              ))}
            </div>
          </div>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════ PROJECTS ═══════ -->
  <section class="section" id="projects">
    <div class="section-inner">
      <p class="section-label" data-reveal>Open Source</p>
      <h2 class="section-title" data-reveal>Side Projects and Learning Tools</h2>
      <p class="section-desc" data-reveal style="margin-bottom: 1rem; max-width: 700px;">
        {data.projects.description}
      </p>
      <a href={data.projects.suiteLink} target="_blank" rel="noopener" class="project-suite-link" data-reveal>
        <svg viewBox="0 0 24 24" fill="none" xmlns="http://www.w3.org/2000/svg" width="18" height="18"><rect x="2" y="4" width="20" height="7" rx="1.5" stroke="currentColor" stroke-width="1.5" fill="none" opacity="0.8"/><rect x="2" y="13" width="20" height="7" rx="1.5" stroke="currentColor" stroke-width="1.5" fill="none" opacity="0.8"/><line x1="7" y1="11" x2="7" y2="13" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><line x1="12" y1="11" x2="12" y2="13" stroke="currentColor" stroke-width="2" stroke-linecap="round"/><line x1="17" y1="11" x2="17" y2="13" stroke="currentColor" stroke-width="2" stroke-linecap="round"/></svg>
        {data.projects.suiteLabel} →
      </a>

      <div class="project-showcase" data-reveal>
        {data.projects.items.map((project: any) => (
          <a href={project.href} target="_blank" rel="noopener" class="project-card">
            <div class="project-visual">
              <div class="browser-chrome">
                <div class="browser-dots">
                  <span style="background:#ff5f57"></span>
                  <span style="background:#febc2e"></span>
                  <span style="background:#28c840"></span>
                </div>
                <div class="browser-url">{project.browserUrl}</div>
              </div>
              <div class="browser-content">
                <div style="height: 220px; background: var(--bg-elevated); display: flex; align-items: center; justify-content: center;">
                  <span style="font-size: 3rem; opacity: 0.3;">⚡</span>
                </div>
              </div>
            </div>
            <div class="project-info">
              <div class="project-tags">
                {project.tags.map((tag: any) => (
                  <span class={`tag tag-${tag.color}`}>{tag.label}</span>
                ))}
              </div>
              <h3 class="project-name">{project.name}</h3>
              <p class="project-desc">{project.description}</p>
              <span class="project-link">Launch Tool →</span>
            </div>
          </a>
        ))}
      </div>
    </div>
  </section>

  <!-- ═══════ CONTENT ═══════ -->
  <section class="section" id="content">
    <div class="section-inner">
      <p class="section-label" data-reveal>Content</p>
      <h2 class="section-title" data-reveal>Videos and Posts</h2>
      <p class="section-desc" data-reveal>{data.content.description}</p>

      <div class="linkedin-embeds" data-reveal>
        {data.content.embeds.map((embedUrl: string) => (
          <div class="linkedin-embed-card">
            <iframe src={embedUrl} height="550" width="504" frameborder="0" allowfullscreen title="Embedded post" loading="lazy"></iframe>
          </div>
        ))}
      </div>

      <div style="margin-top: 2.5rem; text-align: center;" data-reveal>
        <a href={data.content.linkedinUrl} target="_blank" rel="noopener" class="btn btn-primary">💼 Follow on LinkedIn →</a>
      </div>
    </div>
  </section>

  <!-- ═══════ WORK WITH ME ═══════ -->
  <section class="section" id="services">
    <div class="section-inner">
      <p class="section-label" data-reveal>Work With Me</p>
      <h2 class="section-title" data-reveal>Mentorship and Guidance</h2>
      <p class="section-desc" data-reveal style="margin-bottom: 1rem;" set:html={`<strong>${data.services.stats.split(' · ')[0]}</strong> · <strong>${data.services.stats.split(' · ')[1]}</strong> · <strong>${data.services.stats.split(' · ')[2]}</strong>`} />

      <div class="services-grid" data-reveal>
        {data.services.items.map((service: any) => (
          <a href={data.services.bookingLink} target="_blank" rel="noopener" class="service-card">
            <span class="service-icon">{service.icon}</span>
            <h4 class="service-title">{service.title}</h4>
            <p class="service-desc">{service.description}</p>
          </a>
        ))}
      </div>

      <!-- Testimonials -->
      <div class="testimonials" data-reveal>
        <div class="testimonial-track" id="testimonial-track">
          {[...data.testimonials, ...data.testimonials].map((text: string) => (
            <div class="testimonial">
              <p>"{text}"</p>
              <span class="testimonial-rating">★★★★★</span>
            </div>
          ))}
        </div>
      </div>
    </div>
  </section>

  <!-- ═══════ CONTACT ═══════ -->
  <section class="section" id="contact">
    <div class="section-inner">
      <p class="section-label" data-reveal>Contact</p>
      <h2 class="section-title" data-reveal>Let's Connect</h2>
      <p class="section-desc" data-reveal>
        Interested in VLSI tools, interactive education, or collaboration?
      </p>
      <div class="contact-chips" data-reveal>
        <a href={data.services.bookingLink} target="_blank" rel="noopener" class="chip chip-primary">🎯 Book a Session</a>
        <a href={data.social.github} target="_blank" rel="noopener" class="chip">⚡ GitHub</a>
        <a href={data.social.linkedin} target="_blank" rel="noopener" class="chip">💼 LinkedIn</a>
      </div>
    </div>
  </section>

  <!-- ═══════ FOOTER ═══════ -->
  <footer class="footer">
    <div class="footer-left">© 2026 Akarshan Arora</div>
    <div class="footer-right">Built with Astro</div>
  </footer>

  <!-- ═══════ GSAP + LENIS ANIMATIONS ═══════ -->
  <script>
    import { gsap } from 'gsap';
    import { ScrollTrigger } from 'gsap/ScrollTrigger';
    import Lenis from '@studio-freight/lenis';

    gsap.registerPlugin(ScrollTrigger);

    // ═══════════════════════════════════════
    // 1. SMOOTH SCROLL (Lenis)
    // ═══════════════════════════════════════
    const lenis = new Lenis({
      duration: 1.4,
      easing: (t) => Math.min(1, 1.001 - Math.pow(2, -10 * t)),
      smoothWheel: true,
    });
    lenis.on('scroll', ScrollTrigger.update);
    gsap.ticker.add((time) => { lenis.raf(time * 1000); });
    gsap.ticker.lagSmoothing(0);

    // ═══════════════════════════════════════
    // 2. CUSTOM CURSOR + MAGNETIC EFFECT
    // ═══════════════════════════════════════
    const cursor = document.getElementById('cursor');
    const cursorDot = document.getElementById('cursor-dot');
    let cx = -100, cy = -100, dx = -100, dy = -100;

    document.addEventListener('mousemove', (e) => {
      cx = e.clientX;
      cy = e.clientY;
      gsap.to(cursorDot, { x: cx, y: cy, duration: 0.08, ease: 'power2.out' });
    });

    gsap.ticker.add(() => {
      dx += (cx - dx) * 0.1;
      dy += (cy - dy) * 0.1;
      gsap.set(cursor, { x: dx, y: dy });
    });

    // Hover scaling on interactive elements
    document.querySelectorAll('a, button, .btn, .chip, .stat, .project-card, .service-card, .timeline-item').forEach((el) => {
      el.addEventListener('mouseenter', () => cursor.classList.add('hovering'));
      el.addEventListener('mouseleave', () => cursor.classList.remove('hovering'));
    });

    // Magnetic pull effect on buttons
    document.querySelectorAll('.btn, .chip').forEach((btn) => {
      btn.addEventListener('mousemove', (e) => {
        const rect = btn.getBoundingClientRect();
        const bx = rect.left + rect.width / 2;
        const by = rect.top + rect.height / 2;
        const pullX = (e.clientX - bx) * 0.2;
        const pullY = (e.clientY - by) * 0.2;
        gsap.to(btn, { x: pullX, y: pullY, duration: 0.4, ease: 'power3.out' });
      });
      btn.addEventListener('mouseleave', () => {
        gsap.to(btn, { x: 0, y: 0, duration: 0.6, ease: 'elastic.out(1, 0.4)' });
      });
    });

    // ═══════════════════════════════════════
    // 3. LOADING SCREEN - cinematic reveal
    // ═══════════════════════════════════════
    const loader = document.getElementById('loader');
    const loaderChars = loader.querySelectorAll('.loader-text span');
    const lineEl = loader.querySelector('.loader-line');

    const loadTl = gsap.timeline();
    loadTl
      .set(loaderChars, { y: '120%', rotateX: -80 })
      .to(loaderChars, {
        y: '0%',
        rotateX: 0,
        duration: 0.8,
        stagger: 0.035,
        ease: 'power4.out',
      })
      .fromTo(lineEl,
        { scaleX: 0 },
        { scaleX: 1, duration: 0.8, ease: 'power2.inOut', transformOrigin: 'left center' },
        '-=0.3'
      )
      .to(loaderChars, {
        y: '-120%',
        rotateX: 80,
        duration: 0.5,
        stagger: 0.02,
        ease: 'power3.in',
      }, '+=0.4')
      .to(lineEl, { scaleX: 0, duration: 0.3, transformOrigin: 'right center' }, '-=0.3')
      .to(loader, {
        clipPath: 'inset(0 0 100% 0)',
        duration: 1,
        ease: 'power4.inOut',
      })
      .set(loader, { display: 'none' });

    // ═══════════════════════════════════════
    // 4. HERO - word-by-word reveal + parallax
    // ═══════════════════════════════════════
    const heroTl = gsap.timeline({ delay: 2.5 });
    heroTl
      .to('#nav', { opacity: 1, duration: 0.8, ease: 'power2.out' })
      .fromTo('#hero-label',
        { opacity: 0, y: 20, scale: 0.95 },
        { opacity: 1, y: 0, scale: 1, duration: 0.7, ease: 'power3.out' },
        '-=0.5'
      )
      .to('.word-inner', {
        y: '0%',
        duration: 1.2,
        stagger: 0.06,
        ease: 'power4.out',
      }, '-=0.4')
      .fromTo('#hero-sub',
        { opacity: 0, y: 30 },
        { opacity: 1, y: 0, duration: 0.8, ease: 'power3.out' },
        '-=0.6'
      )
      .fromTo('#hero-cta',
        { opacity: 0, y: 20 },
        { opacity: 1, y: 0, duration: 0.7, ease: 'power3.out' },
        '-=0.5'
      )
      .fromTo('#hero-scroll',
        { opacity: 0 },
        { opacity: 1, duration: 0.6 },
        '-=0.2'
      );

    // Hero elements parallax on scroll — different speeds create depth
    gsap.to('#hero-label', {
      yPercent: -50, opacity: 0,
      scrollTrigger: { trigger: '#hero', start: 'top top', end: '60% top', scrub: 0.8 }
    });
    gsap.to('#hero-title', {
      yPercent: -40, scale: 0.92, opacity: 0,
      scrollTrigger: { trigger: '#hero', start: 'top top', end: '80% top', scrub: 0.8 }
    });
    gsap.to('#hero-sub', {
      yPercent: -30, opacity: 0,
      scrollTrigger: { trigger: '#hero', start: '20% top', end: '70% top', scrub: 0.8 }
    });
    gsap.fromTo('#hero-cta',
      { yPercent: 0, opacity: 1 },
      {
        yPercent: -20, opacity: 0,
        scrollTrigger: { trigger: '#hero', start: '30% top', end: '70% top', scrub: 0.8 }
      }
    );
    gsap.to('#hero-scroll', {
      opacity: 0,
      scrollTrigger: { trigger: '#hero', start: '10% top', end: '30% top', scrub: true }
    });

    // ═══════════════════════════════════════
    // 5. MARQUEE - scroll-speed reactive
    // ═══════════════════════════════════════
    const track = document.getElementById('marquee-track');
    const trackWidth = track.scrollWidth / 2;
    const marqueeAnim = gsap.to(track, {
      x: -trackWidth,
      duration: 25,
      ease: 'none',
      repeat: -1,
    });
    // Speed up marquee based on scroll velocity
    ScrollTrigger.create({
      trigger: '.marquee',
      start: 'top bottom',
      end: 'bottom top',
      onUpdate: (self) => {
        const speed = 1 + Math.abs(self.getVelocity() / 3000);
        gsap.to(marqueeAnim, { timeScale: speed, duration: 0.3, overwrite: true });
      }
    });

    // ═══════════════════════════════════════
    // 6. SECTION REVEALS - clip + transform
    // ═══════════════════════════════════════
    // Section labels: slide in from left with line
    document.querySelectorAll('.section-label').forEach((el) => {
      gsap.fromTo(el,
        { x: -30, opacity: 0 },
        {
          x: 0, opacity: 1, duration: 0.8, ease: 'power3.out',
          scrollTrigger: { trigger: el, start: 'top 88%' },
        }
      );
    });

    // Section titles: character split reveal
    document.querySelectorAll('.section-title').forEach((title) => {
      const text = title.innerHTML;
      // Wrap each character in a span (preserve HTML tags like <br/>)
      title.innerHTML = text.replace(/(<[^>]+>)|(.)/g, (match, tag, char) => {
        if (tag) return tag;
        if (char === ' ') return ' ';
        return `<span class="char" style="display:inline-block;opacity:0;transform:translateY(30px)">${char}</span>`;
      });
      const chars = title.querySelectorAll('.char');
      gsap.to(chars, {
        y: 0, opacity: 1, duration: 0.5, stagger: 0.015, ease: 'power3.out',
        scrollTrigger: { trigger: title, start: 'top 85%' },
      });
    });

    // Section desc: fade + slight rise
    document.querySelectorAll('.section-desc').forEach((el) => {
      gsap.fromTo(el,
        { y: 40, opacity: 0 },
        {
          y: 0, opacity: 1, duration: 1, ease: 'power3.out',
          scrollTrigger: { trigger: el, start: 'top 85%' },
        }
      );
    });

    // ═══════════════════════════════════════
    // 7. ABOUT STATS - counter + scale entrance
    // ═══════════════════════════════════════
    document.querySelectorAll('.stat').forEach((stat, i) => {
      gsap.fromTo(stat,
        { y: 60, opacity: 0, scale: 0.9 },
        {
          y: 0, opacity: 1, scale: 1,
          duration: 0.8,
          ease: 'back.out(1.4)',
          delay: i * 0.1,
          scrollTrigger: { trigger: '.about-stats', start: 'top 82%' },
        }
      );
    });

    // ═══════════════════════════════════════
    // 8. EXPERIENCE TIMELINE - scroll-driven line + stagger
    // ═══════════════════════════════════════
    // Animate the timeline line growing as you scroll
    const timeline = document.querySelector('.timeline');
    if (timeline) {
      gsap.fromTo(timeline,
        { '--line-progress': '0%' },
        {
          '--line-progress': '100%',
          ease: 'none',
          scrollTrigger: {
            trigger: timeline,
            start: 'top 70%',
            end: 'bottom 50%',
            scrub: 0.5,
          }
        }
      );

      document.querySelectorAll('.timeline-item').forEach((item, i) => {
        // Slide from left with rotation
        gsap.fromTo(item,
          { x: -60, opacity: 0, rotateY: -5 },
          {
            x: 0, opacity: 1, rotateY: 0,
            duration: 0.9,
            ease: 'power3.out',
            delay: i * 0.12,
            scrollTrigger: { trigger: item, start: 'top 85%' },
          }
        );

        // Dot pulse animation on enter
        const dot = item.querySelector('.timeline-dot');
        gsap.fromTo(dot,
          { scale: 0 },
          {
            scale: 1,
            duration: 0.5,
            ease: 'elastic.out(1.2, 0.5)',
            delay: i * 0.12 + 0.3,
            scrollTrigger: { trigger: item, start: 'top 85%' },
          }
        );

        // Chips stagger
        const chips = item.querySelectorAll('.timeline-chip');
        gsap.fromTo(chips,
          { y: 10, opacity: 0, scale: 0.9 },
          {
            y: 0, opacity: 1, scale: 1,
            duration: 0.4,
            stagger: 0.05,
            ease: 'power3.out',
            delay: i * 0.12 + 0.5,
            scrollTrigger: { trigger: item, start: 'top 85%' },
          }
        );
      });
    }

    // ═══════════════════════════════════════
    // 9. PROJECT CARDS - dramatic scale entrance + internal parallax
    // ═══════════════════════════════════════
    const projCards = document.querySelectorAll('.project-card');
    projCards.forEach((projCard, i) => {
      gsap.fromTo(projCard,
        { y: 100, opacity: 0, scale: 0.95 },
        {
          y: 0, opacity: 1, scale: 1,
          duration: 1.2,
          delay: i * 0.15,
          ease: 'power3.out',
          scrollTrigger: { trigger: '.project-showcase', start: 'top 80%' },
        }
      );
      // Internal parallax: visual moves slower than info
      const visual = projCard.querySelector('.project-visual');
      if (visual) {
        gsap.to(visual, {
          yPercent: -8,
          scrollTrigger: {
            trigger: projCard,
            start: 'top bottom',
            end: 'bottom top',
            scrub: 0.6,
          }
        });
      }
      // Tags stagger in
      gsap.fromTo(projCard.querySelectorAll('.project-tags .tag'),
        { y: 15, opacity: 0, scale: 0.8 },
        {
          y: 0, opacity: 1, scale: 1,
          duration: 0.5,
          stagger: 0.06,
          ease: 'back.out(2)',
          scrollTrigger: { trigger: projCard, start: 'top 75%' },
        }
      );
    });

    // ═══════════════════════════════════════
    // 10. SERVICE CARDS - 3D flip-in stagger
    // ═══════════════════════════════════════
    document.querySelectorAll('.service-card').forEach((card, i) => {
      gsap.fromTo(card,
        { y: 60, opacity: 0, rotateX: -10, transformPerspective: 800 },
        {
          y: 0, opacity: 1, rotateX: 0,
          duration: 0.8,
          ease: 'power3.out',
          delay: i * 0.07,
          scrollTrigger: { trigger: '.services-grid', start: 'top 80%' },
        }
      );
    });

    // ═══════════════════════════════════════
    // 11. TESTIMONIALS - auto-scrolling continuous loop
    // ═══════════════════════════════════════
    const testTrack = document.getElementById('testimonial-track');
    const testSection = document.querySelector('.testimonials');
    if (testTrack && testSection) {
      const testWidth = testTrack.scrollWidth / 2;
      gsap.to(testTrack, {
        x: -testWidth,
        duration: 25,
        ease: 'none',
        repeat: -1,
      });
    }

    // ═══════════════════════════════════════
    // 12. CONTACT - chips bounce in
    // ═══════════════════════════════════════
    document.querySelectorAll('.chip').forEach((chip, i) => {
      gsap.fromTo(chip,
        { y: 30, opacity: 0, scale: 0.85 },
        {
          y: 0, opacity: 1, scale: 1,
          duration: 0.6,
          ease: 'back.out(1.8)',
          delay: i * 0.08,
          scrollTrigger: { trigger: '.contact-chips', start: 'top 85%' },
        }
      );
    });

    // ═══════════════════════════════════════
    // 13. SECTION PARALLAX - different scroll speeds per section
    // ═══════════════════════════════════════
    document.querySelectorAll('.section').forEach((section) => {
      const inner = section.querySelector('.section-inner');
      if (!inner) return;
      gsap.fromTo(inner,
        { yPercent: 5 },
        {
          yPercent: -5,
          ease: 'none',
          scrollTrigger: {
            trigger: section,
            start: 'top bottom',
            end: 'bottom top',
            scrub: 0.4,
          }
        }
      );
    });
  </script>
</body>
</html>
