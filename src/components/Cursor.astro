---
/**
 * Custom cursor elements — include on every page.
 * Requires a page-level script to drive the GSAP animation.
 * Hidden automatically on touch devices and reduced-motion via global CSS.
 */
---
<div class="cursor" id="cursor" aria-hidden="true"></div>
<div class="cursor-dot" id="cursor-dot" aria-hidden="true"></div>
