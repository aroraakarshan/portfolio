<!-- AUTO-GENERATED by llm-wiki. Do not edit directly. -->
<!-- Source: portfolio.yaml | Pages: 11 -->
<!-- Regenerate: python3 ~/llm-wiki/generate-instructions.py -->

## Global

# Global Preferences

These preferences apply across all projects.

## Code Style
- Prefer clear, readable code over clever one-liners
- Use meaningful variable and function names
- Keep functions small and focused — single responsibility
- Add comments only when the *why* is not obvious from the code

## Communication
- Be concise in explanations
- Show code changes rather than describing them
- When suggesting alternatives, explain trade-offs briefly

## Project Context
- Author: Akarshan Arora
- Domain: VLSI Engineering & Web Development
- Common tech: TypeScript, Svelte, Astro, React, Python

## Active Projects

| Project | Stack | Purpose |
|---------|-------|---------|
| pdn-from-zero | SvelteKit, mdsvex, Tailwind | Interactive PDN education site |
| yaml-from-zero | SvelteKit, mdsvex, Tailwind, Shiki | Interactive YAML education site |
| pdn-toy-tools | SvelteKit, Three.js, D3 | PDN visualization tools |
| portfolio | Astro, React, GSAP | Personal portfolio site |
| remotion-videos | Remotion, React, Three.js | Educational video creation |
| vlsi-engineering-books | Sphinx, MyST, Python | VLSI engineering book collection |
| misc | Any | Catch-all for general learnings, tooling, small projects |

---

# Knowledge Vault — Auto-Capture Rules

A knowledge vault lives at `~/llm-wiki/`. You MUST update it during sessions that produce durable learnings. This is not optional.

## When to Write

Write to the vault when any of these occur during a session:
- A **bug or gotcha** is discovered and fixed (especially non-obvious ones)
- A **pattern or technique** is learned that has future reuse value
- An **architectural decision** is made with trade-offs worth recording
- A **tool quirk or workaround** is found (build tools, libraries, APIs)
- A **debugging approach** worked (or didn't) in a useful way
- A **configuration** took effort to get right

Do NOT wait until the end of the session. Write at **natural checkpoints** — once a learning is confirmed and stable, record it. Multiple vault updates per session are normal and expected.

Skip vault edits ONLY if: the session is purely read-only/review, no durable learning was produced, or the user explicitly says not to.

## Where to Write

The vault is organized by project:

```
~/llm-wiki/
├── projects/{project}/             ← Project-specific knowledge
│   ├── {topic}.md                  ← Topic pages (architecture, tools, patterns)
│   └── patterns/{category}.md      ← Error patterns, gotchas, fixes
├── projects/misc/                  ← Catch-all: general tooling, Linux, git, small projects
├── shared/                         ← Cross-project knowledge
└── projects/general/global.md      ← This file (global preferences)
```

**Routing rules:**
- If the learning is specific to the current project → write to `~/llm-wiki/projects/{project}/`
- If it applies across projects → write to `~/llm-wiki/shared/`
- If you're not in a recognized project, or the learning is general (Linux, git, shell, tooling) → write to `~/llm-wiki/projects/misc/`
- For bug/gotcha patterns → write to `~/llm-wiki/projects/{project}/patterns/`

## How to Write

### For gotchas and error patterns (add to `patterns/*.md`):
```markdown
## Short Descriptive Title
What happened, why it was confusing, and the fix.
**Fix**: The specific solution or workaround.
**Applies when**: Context for when this matters.
```

### For general knowledge (add to or create topic pages):
- Add to an existing topic page if one fits
- Create a new `.md` file if the topic is new
- Use `wiki-links` to cross-reference other pages
- Add `#tags` in frontmatter for discoverability

### After writing to the vault:
Run the generator **once** after your last vault edit of the session:
```bash
python3 ~/llm-wiki/generate-instructions.py
```
This compiles wiki pages into each project's `.github/copilot-instructions.md`. The script uses absolute paths and works from any directory.

Do NOT run the generator after every small edit — batch edits and regenerate once before concluding vault work for the session.

## Access Note
The vault (`~/llm-wiki/`) may be outside the current workspace. If file access is restricted or denied, include the learning in your response text so the user can manually add it.

---

##  Moc

# Portfolio — Map of Content

Personal portfolio site for Akarshan Arora (VLSI Engineer & Developer). Built with Astro 6 + React islands, featuring cinematic animations, particle effects, and circuit board visuals.

## Architecture
- astro-architecture — Pages, components, config, build
- react-islands — React components in Astro (`client:only`)
- gsap-animation — GSAP + ScrollTrigger animations

## Design
- responsive-design — Layout, smooth scrolling, custom UX
- visual-effects — Particle canvas, circuit board, wave dividers

## Patterns & Learnings
- patterns/animation — Animation-related patterns and fixes
- patterns/layout — Layout and responsive design patterns

## Quick Reference
- **Dev server**: `bun run dev`
- **Build**: `bun run build`
- **Preview**: `bun run preview`
- **Pages**: `src/pages/`
- **Components**: `src/components/`
- **Data**: `src/data/portfolio.json`
- **Styles**: `src/styles/global.css`

---

## Astro Architecture

# Astro Architecture

## Project Structure
```
src/
├── pages/
│   └── index.astro         ← Main (single-page) portfolio
├── components/
│   ├── CircuitBoard.astro   ← SVG circuit board background
│   ├── WaveDivider.astro    ← SVG wave section dividers
│   └── ParticleCanvas.tsx   ← React particle effect (client:only)
├── data/
│   └── portfolio.json       ← All portfolio content (projects, experience, etc.)
└── styles/
    └── global.css           ← Global styles (~850 lines)
```

## Astro Config
- `astro.config.mjs` — React integration (`@astrojs/react`)
- Node >= 22.12.0 required
- Bun as package manager

## Key Concepts
- **Island Architecture**: Static HTML with hydrated React islands
- **`client:only="react"`**: Used for browser-only components (ParticleCanvas)
- **Content in JSON**: All portfolio data centralized in `portfolio.json`
- **Single-page design**: Everything on `index.astro` with scroll-based sections

---

## Gsap Animation

# GSAP Animation

## Setup
- GSAP + ScrollTrigger for scroll-driven animations
- Lenis for smooth scrolling behavior
- Animations defined in `index.astro` `<script>` blocks

## ScrollTrigger Patterns
- Trigger animations as sections enter viewport
- Pin sections for extended scroll interactions
- Scrub animations tied to scroll progress

## Lenis Smooth Scroll
- Provides buttery smooth scrolling UX
- Integrated with GSAP's ScrollTrigger for accurate triggering
- Handles momentum and easing

## Animation Guidelines
- Keep animations purposeful — enhance understanding, don't distract
- Use `will-change` CSS property sparingly for GPU-accelerated elements
- Test on mobile — reduce or disable heavy animations on small screens
- Respect `prefers-reduced-motion` media query

## Common Gotchas

## Hero elements flicker on load — fromTo + scrub conflict
Using `gsap.fromTo()` for both entrance animation AND scroll parallax on the same element causes a double-render flicker. The scroll parallax `fromTo` immediately applies its "from" values (opacity: 1), overriding the entrance animation's initial state (opacity: 0).
**Fix**: Add `immediateRender: false` to the scroll parallax `fromTo`. This prevents it from applying "from" values at creation time, while still allowing the scrub to reverse properly on scroll-up.
**Applies when**: Any element has both a timeline entrance animation and a scroll-triggered parallax.

## Hero elements disappear when scrolling back up
Changing a scroll `fromTo` to `gsap.to()` captures the CSS initial value (opacity: 0) as the start. Scrolling back up restores to opacity: 0 — permanently hidden.
**Fix**: Keep `fromTo` (so GSAP knows both start and end values for scrubbing) but add `immediateRender: false`.
**Applies when**: Elements that start hidden via CSS and are revealed by a timeline, then have scroll parallax.

## SVG animations not visible — CSS clip-path approach fails
Using CSS `clip-path: inset(0 100% 0 0)` with CSS transitions doesn't work reliably alongside GSAP ScrollTrigger.
**Fix**: Drive all SVG animations through GSAP directly. Use `getTotalLength()` for stroke-dashoffset draw effects. Don't mix CSS transitions with GSAP on the same elements.
**Applies when**: SVG line-draw or reveal animations on the portfolio.

---

## React Islands

# React Island Components

## Overview
React components run as hydrated islands within the static Astro site.

## ParticleCanvas
- Location: `src/components/ParticleCanvas.tsx`
- Interactive particle effect on the hero section
- Uses HTML Canvas API with `requestAnimationFrame`
- Must use `client:only="react"` (no SSR — relies on `window`/`canvas`)
- ~130 lines, handles resize, mouse interaction, particle physics

## Integration Pattern
```astro
---
import ParticleCanvas from '../components/ParticleCanvas.tsx';
---
<ParticleCanvas client:only="react" />
```

## Guidelines
- Only use React for components that need rich interactivity or Canvas
- Prefer Astro components (`.astro`) for everything else
- `client:only` means no server rendering — component only loads in browser
- Keep React islands small and self-contained

---

## Responsive Design

# Responsive Design

## Layout Strategy
- Single-page scroll design with distinct sections
- Full-viewport hero with particle canvas background
- Section dividers using SVG wave components
- Circuit board visual as decorative background element

## Custom UX Elements
- Custom cursor effects
- Loading states/transitions
- Scroll-driven section reveals

## Responsive Approach
- Mobile-first CSS with Tailwind-style breakpoints
- Global CSS in `src/styles/global.css` (~850 lines)
- Heavy use of CSS custom properties for theming
- Flexible grid layouts for project cards and content sections

## Visual Effects
- `ParticleCanvas.tsx` — interactive particle field (React/Canvas)
- `CircuitBoard.astro` — SVG circuit board pattern
- `WaveDivider.astro` — SVG wave section separators

---

## Animation

# Animation Patterns — portfolio

## Error Patterns & Fixes

## Horizontal scroll pin conflicts with vertical entrance animations
When ScrollTrigger pins a section for horizontal scroll, existing vertical entrance animations on child elements (from `start: 'top 85%'`) won't fire correctly because the children are moving horizontally, not vertically.
**Fix**: Use `containerAnimation` on child ScrollTriggers to bind them to the horizontal tween. Use `start: 'left 85%'` instead of `start: 'top 85%'`. Exclude pinned sections from generic `.section` parallax.
**Applies when**: Any section uses ScrollTrigger pin + horizontal scroll with animated children.

## 3D tilt overwrites CSS hover transforms
If CSS `:hover` sets `transform: translateY(-6px)` and JS also sets `transform` via GSAP on mousemove, they fight. One cancels the other.
**Fix**: Remove CSS hover transform and let GSAP handle all transforms (including the translateY) in one place. Use `overwrite: 'auto'` on GSAP tweens.
**Applies when**: Any element has both CSS hover transforms and GSAP mousemove transforms.

## Loader delay vs hero reveal — don't use fixed `delay`
Using `gsap.timeline({ delay: 2.5 })` for hero reveal creates dead air if loader is faster, or cuts off if loader is slower.
**Fix**: Chain both loader and hero into a single `masterTl` timeline so hero reveal starts immediately after loader exits.
**Applies when**: Any loader/intro sequence followed by content reveal.

---

## Layout

# Layout Patterns — portfolio

## Error Patterns & Fixes
<!-- Record layout/CSS/responsive issues and solutions here -->

<!-- Example format:
## Content overflow on mobile Safari
Fixed elements and 100vh caused content to extend behind the address bar.
**Fix**: Use `100dvh` (dynamic viewport height) instead of `100vh`.
-->

---

## Typescript Patterns

# TypeScript Patterns

Shared TypeScript patterns across projects.

## Strict Mode
- Enable `strict: true` in all projects
- Avoid `any` — use `unknown` + type guards instead

## Type Patterns
- Prefer `interface` for object shapes, `type` for unions/intersections
- Use discriminated unions for state machines
- `satisfies` operator for type checking without widening

## Import Patterns
- Use path aliases (`$lib`, `@/`) for clean imports
- Barrel exports from `index.ts` for library modules

## Error Handling
- Use `Result<T, E>` pattern or explicit error types over thrown exceptions
- Type-narrow errors in catch blocks: `if (error instanceof SpecificError)`

## Common Gotchas
<!-- Add learnings here as they occur -->

---

## Tailwind Css

# Tailwind CSS Patterns

Shared Tailwind patterns across projects.

## Version Notes
- Most projects use Tailwind CSS v4 with the Vite plugin (`@tailwindcss/vite`)
- v4 uses CSS-first configuration (no `tailwind.config.js`)
- Import via `@import "tailwindcss"` in CSS

## Typography Plugin
- `@tailwindcss/typography` for prose content (lessons, articles)
- Apply with `prose` class on content containers
- Customize with `prose-invert` for dark themes

## Responsive Patterns
- Mobile-first approach
- Key breakpoints: `sm:640px`, `md:768px`, `lg:1024px`, `xl:1280px`
- Use container queries where appropriate

## Dark Mode
- Use `dark:` variant for dark mode styles
- Consider `prefers-color-scheme` for system preference

## Common Gotchas
<!-- Add learnings here as they occur -->

---

## Classy V2 Portable Spec

# Classy-v2 Editorial Aesthetic — Portable Spec

Self-contained design spec for producing charts, diagrams, and UI in a restrained editorial style (think Economist / Stripe Press / NYT graphics). Paste this whole file into any LLM session to give it the context it needs to generate output in this aesthetic.

## One-line statement

Content is the visual. Motion and decoration serve comprehension; they never replace it. When in doubt, delete.

## The smell-test (what cheap output looks like)

If your generated output has 3+ of these, it looks cheap regardless of content quality:

- Background is a gradient (any direction, any color)
- Anything has a blurry glow (`box-shadow: 0 0 Npx <accent>`)
- Particles, floating dots, pulse rings, "data flow lines"
- Text cards spring-bounce into place
- Three-column feature grids as structural device
- Default Tailwind blue (`#3b82f6`) + emerald (`#10b981`)
- Decorative corner brackets framing every section
- Animated scanlines / circuit grids behind content
- Rainbow color cycles (matplotlib's `tab10`, Plotly defaults)
- Excessive `border-radius: 24px` on everything

These are the cliches of the AI-generated landing-page era. Reject them.

## Palettes (8 colors total, max)

### Light
| Token     | Hex       | Use |
|-----------|-----------|-----|
| ink       | `#1A1A1A` | Body text, primary strokes |
| paper     | `#FAFAF7` | Background (warm off-white, not pure) |
| accent    | `#C45D2C` | Signature color — sparing use |
| hairline  | `#E5E5E0` | 1px dividers, grid lines |
| muted     | `#6B6B66` | Subtitle, caption body |
| tertiary  | `#A3A39E` | Footer, eyebrow label |

### Dark
| Token     | Hex       | Use |
|-----------|-----------|-----|
| ink       | `#0B0B0C` | Background |
| paper     | `#F5F2EC` | Primary text |
| accent    | `#D97547` | Signature color (brighter for dark contrast) |
| hairline  | `#2A2A2C` | Dividers, grid |
| muted     | `#9A9994` | Subtitle, caption |
| tertiary  | `#6B6A66` | Footer, eyebrow |

### Alternative signature accents (pick one per project, never mix)
- Terracotta `#C45D2C` (light) / `#D97547` (dark)
- Ochre `#C69A3A` / `#E0B355`
- Deep forest `#3A5A40` / `#6B9471`
- Ink red `#9A3324` / `#C04A35`

### Semantic colors (charts only, meaning-bearing)
- Positive: `#3A5A40`
- Negative: `#9A3324`
- Warn: `#C69A3A`

## Typography

**Two font families max** across a whole project. Add a mono for code and eyebrow labels only.

- Display / body: clean modern sans — **Inter**, **Geist**, **IBM Plex Sans**
- Optional hero face: serif with character — **Fraunces**, **Playfair**, **Recoleta** — used only for big editorial moments
- Mono: **JetBrains Mono**, **Berkeley Mono**, **IBM Plex Mono** — for code AND eyebrow captions (uppercase, tracked `0.18em`–`0.22em`)

**Size scale** (geometric, never arbitrary):
```
22 / 30 / 42 / 60 / 84 / 120 / 200
```

**Letter-spacing** at large sizes:
- 60px+: `-0.02em` to `-0.04em` (default is too loose)
- Eyebrow caps: `+0.18em` to `+0.22em`
- Body: `0` or `+0.01em`

**Weight contrast beats size contrast.** A 56px 900-weight headline against 22px 300-weight sub reads better than two 40px 500-weight texts.

**Numerals**: use tabular figures via `font-feature-settings: "tnum" 1` for anything numeric in tables or charts.

## Layout discipline

- Safe area: ≥6% inset on the shortest edge. Nothing critical at the edge.
- **One primary + one secondary element per frame.** Never three competing focal points.
- Three-column grids are **banned as a structural device**. Replace with: staircase (offset diagonally), sequential reveal, 60/40 asymmetric split, or a single hero with supporting caption.
- Generous whitespace. If it feels too empty, it's probably right.

## Shadows & elevation

- **NO blurry glows.** `box-shadow: 0 0 Npx` with any blur radius reads as cheap SaaS.
- Use **sharp offset shadows**: `box-shadow: 8px 8px 0 0 var(--ink);` (light) or a solid accent rect offset behind the card (dark).
- Hairline borders (1px) substitute for most places you'd reach for a shadow.

## Motion (when motion is in scope)

Static content: skip this section.

- Fade-in: 14–30 frames @ 30fps (≈0.5–1.0s). Below 14f = flash. Above 60f = memory test.
- Fade-out: 12–20 frames.
- Easing: `cubic-bezier(0.22, 1, 0.36, 1)` ("swift") for text. `cubic-bezier(0.65, 0, 0.35, 1)` (symmetric) for diagrams.
- **Springs reserved for numbers/badges only**, never for text cards.
- Y-offset on entry: `-24px`. Text rises into place.
- No infinite loops, no constant ambient motion, no marquees.
- Hold duration: reading time × 1.2 (3–5 word headline = 75–105 frames @ 30fps).

## Chart-specific rules

- Single accent line, not a default color cycle.
- Spines: left + bottom only, at 0.6–0.8px width, in hairline color.
- Grid: off by default. If on, dashed hairline on y-axis only, below content.
- Tick labels in mono (eyebrow-like), muted color, 10pt.
- Number formatter always human-readable: `1.2k`, `3.4M`, `42%` — never raw floats.
- Annotations are the hero. Every editorial chart names its insight with an on-chart callout tied to the data point by a hairline arrow.
- Title/subtitle/eyebrow stack lives **above** the plot area, not inside the axes.

## Authoring checklist

Before declaring any output done:

1. [ ] No gradient anywhere
2. [ ] No blurry glows (any shadow with non-zero blur + zero offset)
3. [ ] Max 2 colors competing for attention in any single frame
4. [ ] Every visible element has a reason to be there (delete decorative)
5. [ ] Typography scale is geometric, not arbitrary
6. [ ] Single signature accent, not a rainbow
7. [ ] Hairline borders (1px) used where others would use shadows
8. [ ] Numbers formatted human-readably (`1.2k` not `1247`)
9. [ ] If a chart: annotations name the insight
10. [ ] If motion: hold durations give reading time × 1.2

## Reference implementations

Companion examples alongside this spec:
- `matplotlib/chart.py` + `chart_dark.py` — annotated line chart, both palettes
- `svg/card.svg` + `card_dark.svg` — hero stat card with sharp offset shadow

The SVG files are also the cleanest demo of what HTML+CSS can do that matplotlib cannot: real letter-spacing, mixed-weight inline text, OpenType numerals, CSS-variable theming.

## How to use this spec with an LLM

Three usable patterns:

1. **Paste-at-start**: drop this whole file as the first message in any new session before asking for design/code output. Any modern model (GPT-4+, Claude 3+, Gemini 1.5+) will honor it for the rest of the conversation.

2. **System prompt / custom instruction**: paste into the "custom instructions" / "system prompt" field of ChatGPT, Claude.ai, or an API client. Persists across all sessions in that tool.

3. **Reference link**: if the LLM has file/URL access, point it at this file (or a gist copy) and ask it to "design per the spec at <path>". Shorter prompt, same effect.

After pasting, a useful opening prompt is: *"Generate X using the palette, typography, and discipline from the spec above. Before writing code, confirm you understand the rules by listing the three you will follow most carefully."* The confirmation step catches models that skimmed.
